/* import { createComments } from './big-picture-view';

const photosContainer = document.querySelector('.pictures');


photosContainer.addEventListener('click', (evt) => {
  console.log(createComments());
}); */
